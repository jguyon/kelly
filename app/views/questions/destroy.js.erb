$('#<%= dom_id @question %>').remove();
<% @moved_questions.each do |question| %>
  $('#<%= dom_id question %> .question-position').
    replaceWith("<%= j render('position', question: question) %>");
<% end %>
